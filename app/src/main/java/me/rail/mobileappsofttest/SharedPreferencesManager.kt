package me.rail.mobileappsofttest

import android.content.Context

class SharedPreferencesManager(context: Context) {
//    private val sharedPreferences =
//        context.getSharedPreferences(PREFERENCES_NAME, Context.MODE_PRIVATE)
//    private val editor = sharedPreferences.edit()
//    private val noteArrayListType = object : TypeToken<ArrayList<Note>?>() {}.type
//
//    var notes: ArrayList<String>?
//        get() = Gson().fromJson(sharedPreferences.getString(NOTES_KEY, ""), noteArrayListType)
//        set(value) = editor.putString(
//            NOTES_KEY, Gson().toJson(value)
//        ).apply()
//
//    companion object {
//        private const val PREFERENCES_NAME = "Mobile Soft App Test Preferences"
//
//        private const val NOTES_KEY = "Notes Key"
//    }
}